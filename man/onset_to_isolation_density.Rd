% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution_plots.R
\name{onset_to_isolation_density}
\alias{onset_to_isolation_density}
\title{Evaluate the onset-to-isolation delay probability density}
\usage{
onset_to_isolation_density(input, id_prefix = "")
}
\arguments{
\item{input}{The Shiny \code{input} reactive of the calling module.}

\item{id_prefix}{A \code{character} string prepended to the onset-to-isolation
input IDs read by this plot (e.g. \code{"dct_"}). Must match the \code{id_prefix}
passed to the corresponding \code{\link[=onset_to_isolation_input]{onset_to_isolation_input()}}. Defaults to \code{""}
(no prefix).}
}
\value{
A \code{data.frame} with numeric columns \code{x} (delay in days) and \code{y}
(density).
}
\description{
Compute the onset-to-isolation delay probability density under the
user-selected parametric distribution (\code{"lnorm"}, \code{"gamma"} or \code{"weibull"})
for a single contact tracing strategy, reading the strategy's prefixed
inputs. Reactive guards (\code{\link[shiny:req]{shiny::req()}}) hold rendering until the relevant
inputs are valid, so this is intended to be called from within a
\code{\link[shiny:renderPlot]{shiny::renderPlot()}} expression.
}
\keyword{internal}
