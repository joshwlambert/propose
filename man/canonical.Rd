% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{canonical}
\alias{canonical}
\title{Canonical parameters describing a single outbreak scenario}
\description{
A \emph{canonical parameter list} is a flat, fixed-order \code{list} of scalars that
fully describes one outbreak scenario, independent of how the user entered
it. It is the interchange format between the sidebar inputs and
\code{\link[ringbp:scenario_sim]{ringbp::scenario_sim()}}:
\itemize{
\item \code{\link[=canonical_params]{canonical_params()}} resolves raw input values into it,
\item \code{\link[=scenario_opts]{scenario_opts()}} turns it back into \pkg{ringbp} option objects,
\item \code{\link[=param_diff]{param_diff()}} compares several of them to report what differs between
scenarios.
}

Two properties make it suitable for the \emph{\strong{Compare}} page:
\enumerate{
\item \strong{It holds values, not closures.} The sampling functions \pkg{ringbp}
needs close over \code{input$...}, so storing them would make an already-added
scenario silently change when the sidebar is edited afterwards. Scalars
snapshot cleanly.
\item \strong{It is mode-resolved and fixed-order.} The Basic and Advanced UI modes
collapse onto the same fields, so two scenarios that describe the same
simulation compare equal under \code{\link[=identical]{identical()}} regardless of which mode was
used to enter them. Fixed field order matters because \code{\link[=identical]{identical()}} on
lists is order-sensitive.
}

\code{NA} has a single, uniform meaning: \strong{this parameter cannot affect this
scenario}, because the UI toggle governing it is off. That covers the
dispersion of a non-Negative-Binomial offspring distribution, the
asymptomatic offspring parameters when asymptomatic transmissibility is not
set separately, and every intervention parameter when isolation is switched
off. Reporting those as \code{NA} keeps them out of the "what differs" table,
where they would otherwise show up as differences that change nothing.
\code{\link[=scenario_opts]{scenario_opts()}} substitutes the model-neutral value for each when it builds
the simulation.
}
