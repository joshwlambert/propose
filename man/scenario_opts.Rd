% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{scenario_opts}
\alias{scenario_opts}
\title{Build the \pkg{ringbp} option objects for a canonical parameter list}
\usage{
scenario_opts(params)
}
\arguments{
\item{params}{A canonical parameter list. See \link{canonical}.}
}
\value{
A \code{list} with elements \code{offspring}, \code{delays}, \code{event_probs} and
\code{interventions}.
}
\description{
Rebuilds the sampling closures from the snapshotted scalars and assembles
the four option objects \code{\link[ringbp:scenario_sim]{ringbp::scenario_sim()}} takes. Two conversions
happen here, both matching the rest of the app: the UI collects percentages
where the model expects proportions, and the time-varying \code{symptomatic_traced}
and \code{test_sensitivity} arguments are wrapped by \code{\link[=npi_activation]{npi_activation()}} so they
only apply from the NPI activation day onwards.

Parameters reported as \code{NA} (see \link{canonical}) are substituted with the value
that makes them inert: no isolation within the simulation horizon
(\link{NO_ISOLATION_DELAY}), no contact tracing, and no test-driven isolation.
}
\keyword{internal}
