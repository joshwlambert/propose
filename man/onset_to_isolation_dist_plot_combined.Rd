% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution_plots.R
\name{onset_to_isolation_dist_plot_combined}
\alias{onset_to_isolation_dist_plot_combined}
\title{Render onset-to-isolation delay densities for several strategies together}
\usage{
onset_to_isolation_dist_plot_combined(
  input,
  id_prefixes,
  palette = NULL,
  toggles = NULL
)
}
\arguments{
\item{input}{The Shiny \code{input} reactive of the calling module.}

\item{id_prefixes}{A named \code{character} vector mapping legend labels (the
names) to input \code{id_prefix}es (the values), e.g.
\code{c(Digital = "dct_", Manual = "mct_", Informal = "ict_")}.}

\item{palette}{A \code{character} vector of colours, one per strategy in the same
order as \code{id_prefixes}, used to colour and order the lines. Pass the same
colours used for the individual per-strategy plots to keep them consistent.
Defaults to \code{NULL} (every line \code{"steelblue"}).}

\item{toggles}{An optional \code{character} vector, the same length and order as
\code{id_prefixes}, of checkbox input IDs that switch each strategy on. Only
strategies whose toggle is \code{TRUE} are drawn. Defaults to \code{NULL} (all drawn).}
}
\value{
Output from \code{\link[shiny:renderPlot]{shiny::renderPlot()}}.
}
\description{
Overlays the onset-to-isolation delay probability density of several contact
tracing strategies on a single grouped line plot, reusing
\code{\link[=onset_to_isolation_density]{onset_to_isolation_density()}} to evaluate each strategy's prefixed inputs.
}
\keyword{internal}
