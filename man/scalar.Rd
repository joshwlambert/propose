% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{scalar}
\alias{scalar}
\alias{as_num_scalar}
\alias{as_chr_scalar}
\title{Coerce a raw input value to a length-one scalar}
\usage{
as_num_scalar(x)

as_chr_scalar(x)
}
\arguments{
\item{x}{A value of length zero or one.}
}
\value{
A length-one \code{numeric} (\code{as_num_scalar()}) or \code{character}
(\code{as_chr_scalar()}), \code{NA} of that type when \code{x} is absent.
}
\description{
Coercion that guarantees a length-one result, which base R's \code{as.numeric()}
and \code{as.character()} do not: a Shiny input that has not yet registered with
the server reads as \code{NULL}, and \code{as.numeric(NULL)} is length \strong{zero} rather
than \code{NA}. A length-zero element would break the canonical parameter list,
whose comparison by \code{\link[=identical]{identical()}} underpins both duplicate-scenario detection
and the "what differs" table. Anything that is not already a single value
becomes \code{NA} of the right type.
}
\keyword{internal}
