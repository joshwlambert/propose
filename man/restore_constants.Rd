% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants.R
\name{restore_constants}
\alias{restore_constants}
\alias{RESTORE_SELECT_IDS}
\alias{RESTORE_RADIO_IDS}
\alias{RESTORE_SWITCH_IDS}
\alias{RESTORE_CHECKBOX_IDS}
\alias{RESTORE_NUMERIC_IDS}
\title{Sidebar inputs describing a scenario, grouped by the function that updates
them}
\usage{
RESTORE_SELECT_IDS

RESTORE_RADIO_IDS

RESTORE_SWITCH_IDS

RESTORE_CHECKBOX_IDS

RESTORE_NUMERIC_IDS
}
\description{
The inputs \code{\link[=apply_params]{apply_params()}} writes back when a scenario is loaded into the
sidebar for editing, split by which \code{shiny::update*Input()} function sets
them. Together these cover every input \code{\link[=canonical_params]{canonical_params()}} reads.

The pathogen preset (\code{pathogen_defaults}) is deliberately \strong{not} restorable.
It is a shortcut for filling the pathogen parameters, not a parameter itself,
and changing it fires the observer that resets all of them — which would
immediately overwrite the scenario being loaded.

Each constant names the inputs restored by one \code{update*Input()} function, so
adding a new sidebar input means adding its ID to whichever list matches the
control it uses.
}
\details{
\strong{\code{RESTORE_SELECT_IDS}} are set with \code{\link[shiny:updateSelectInput]{shiny::updateSelectInput()}}: the
distribution dropdowns.

\strong{\code{RESTORE_RADIO_IDS}} are set with \code{\link[shiny:updateRadioButtons]{shiny::updateRadioButtons()}}: the
Basic/Advanced mode toggles and the Basic-mode variability choices.

\strong{\code{RESTORE_SWITCH_IDS}} are set with \code{\link[bslib:update_switch]{bslib::update_switch()}}: the isolation
master switch and the distinct-asymptomatic-transmissibility toggle.

\strong{\code{RESTORE_CHECKBOX_IDS}} are set with \code{\link[shiny:updateCheckboxInput]{shiny::updateCheckboxInput()}}:
currently just the quarantine checkbox.

\strong{\code{RESTORE_NUMERIC_IDS}} are set with \code{\link[shiny:updateNumericInput]{shiny::updateNumericInput()}}:
everything else, being the reproduction numbers, dispersions, delay
parameters, percentages and intervention values.
}
\keyword{internal}
