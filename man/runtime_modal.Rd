% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime.R
\name{runtime_modal}
\alias{runtime_modal}
\title{Build the confirmation shown before a long-running analysis}
\usage{
runtime_modal(ns, n_sims, seconds)
}
\arguments{
\item{ns}{A namespace created with \code{\link[shiny:NS]{shiny::NS()}}. The modal's buttons are
\code{cancel} and \code{ok} within that namespace, which each page already observes.}

\item{n_sims}{A \code{numeric} scalar: the number of outbreak simulations to run.}

\item{seconds}{A \code{numeric} scalar: the estimated duration (seconds), from
\code{\link[=estimate_runtime]{estimate_runtime()}}.}
}
\value{
A \code{\link[shiny:modalDialog]{shiny::modalDialog()}} object.
}
\description{
The single modal used by every analysis page, so that a confirmation reads the
same way wherever it appears. It reports both the size of the job and how long
it is expected to take, because the number of simulations alone does not tell a
non-coder anything about the wait.

It also states that the app is unresponsive while an analysis runs. That is the
behaviour the \emph{\strong{Proposing Solutions}} workshop reported as the app appearing
broken, and saying so in advance is most of what stops it reading that way.
}
\keyword{internal}
