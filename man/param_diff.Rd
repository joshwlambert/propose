% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{param_diff}
\alias{param_diff}
\title{Split canonical parameters into those that vary between scenarios and those
shared by all of them}
\usage{
param_diff(scenarios)
}
\arguments{
\item{scenarios}{A \code{list} of canonical parameter lists. See \link{canonical}.}
}
\value{
A \code{list} with two \code{character} vectors of parameter names, \code{varying}
and \code{shared}.
}
\description{
Drives the \emph{\strong{Compare}} page's parameter table, whose purpose is to say
plainly what is different between the scenarios being compared.

\code{NA} values are excluded from the comparison rather than treated as a value
of their own, because \code{NA} means the parameter cannot affect that scenario
(see \link{canonical}). A parameter is therefore \emph{varying} only when it takes two
or more different values among the scenarios it applies to, \emph{shared} when it
takes one, and neither when it applies to none of them.
}
\keyword{internal}
