% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{collect_params}
\alias{collect_params}
\title{Collect the sidebar inputs of the calling module into a canonical parameter
list}
\usage{
collect_params(input)
}
\arguments{
\item{input}{The Shiny \code{input} reactive of the calling module.}
}
\value{
A named \code{list} of scalars. See \link{canonical}.
}
\description{
The \pkg{shiny}-facing wrapper around \code{\link[=canonical_params]{canonical_params()}}. It applies the
same range guards the \emph{\strong{Explore}} page applies before simulating, so it
must be called from a reactive context. \code{\link[=canonical_params]{canonical_params()}} itself is pure
and takes a plain \code{list}, which keeps the resolution logic testable without
a Shiny session.
}
\keyword{internal}
