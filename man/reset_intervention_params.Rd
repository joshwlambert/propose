% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{reset_intervention_params}
\alias{reset_intervention_params}
\title{Reset the intervention parameters to default values}
\usage{
reset_intervention_params(session)
}
\arguments{
\item{session}{The \code{session} object passed to function given to
\code{shinyServer}. Default is \code{getDefaultReactiveDomain()}.}
}
\value{
Nothing, called for side-effects from \verb{shiny::update*()} functions.
}
\description{
The counterpart to \code{\link[=reset_pathogen_params]{reset_pathogen_params()}} for the parameters that describe
the response rather than the pathogen: the onset-to-isolation delay, contact
tracing, isolation, quarantine, test sensitivity and the NPI activation day.
Shared by the \emph{\strong{Explore}} and \emph{\strong{Compare}} pages, which offer the same
"Reset Defaults" action over the same sidebar.

The simulation controls (\code{cap_max_days}, \code{cap_cases}, \code{replicates} and
\code{initial_cases}) are left to the caller, because pages differ in which of
them they expose and at what defaults.
}
\keyword{internal}
