% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cap_scenario}
\alias{cap_scenario}
\title{Clamp a simulated outbreak at the maximum-cases cap for plotting}
\usage{
cap_scenario(scenario, cap_cases)
}
\arguments{
\item{scenario}{A \link[data.table:data.table]{data.table::data.table} returned by
\code{\link[ringbp:scenario_sim]{ringbp::scenario_sim()}}.}

\item{cap_cases}{A \code{numeric} scalar, the maximum cumulative cases the
simulation was run with.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with \code{cumulative} clamped and
\code{weekly_cases} recomputed to match.
}
\description{
A replicate can overshoot \code{cap_cases} in the week it reaches the cap, because
the whole of that week's transmission is generated before the simulation
stops. Plotting the raw values would show trajectories rising above a cap the
plot also draws as a guide line. Extinct and uncapped replicates are
unchanged.

Copies before mutating: \code{scenario} is normally a cached reactive value, and
\pkg{data.table} would otherwise modify it in place.
}
\keyword{internal}
