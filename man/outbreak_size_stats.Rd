% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{outbreak_size_stats}
\alias{outbreak_size_stats}
\title{Total outbreak size across replicates, with the spread around it}
\usage{
outbreak_size_stats(scenario, cap_cases)
}
\arguments{
\item{scenario}{A \link[data.table:data.table]{data.table::data.table} returned by
\code{\link[ringbp:scenario_sim]{ringbp::scenario_sim()}}, or \code{NULL}.}

\item{cap_cases}{A \code{numeric} scalar, the maximum cumulative cases the
simulation was run with.}
}
\value{
A \code{list} with elements \code{median}, \code{lower}, \code{upper} and \code{capped}, or
\code{NULL} if \code{scenario} is \code{NULL}.
}
\description{
The number of cases each replicate reached by the end of the simulation,
which is to say after \code{cap_max_days} days. Reported as a median with a 95\%
interval rather than a mean: the distribution mixes outbreaks that were
controlled early with outbreaks that grew unchecked, so the mean sits between
the two and describes neither.

Sizes are taken \strong{uncapped}. A replicate can finish above \code{cap_cases},
because the whole of the week in which it crosses the cap is generated before
the simulation stops, and clamping it would report the cap as though it were
a result. \code{capped} instead reports what proportion of replicates reached the
cap, so a caller can say that those outbreaks were still growing when
counting stopped rather than presenting a number the cap chose.
}
\keyword{internal}
