% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delays_input.R
\name{delays_input}
\alias{delays_input}
\title{Generate \code{\link[bslib:accordion]{bslib::accordion()}} with inputs for parameterising the
\code{incubation_period} or \code{onset_to_isolation} argument in
\code{\link[ringbp:delay_opts]{ringbp::delay_opts()}}}
\usage{
delays_input(
  ns,
  delay_type = c("incubation", "onset_to_isolation"),
  id_prefix = "",
  ...
)
}
\arguments{
\item{ns}{A namespace created with \code{\link[shiny:NS]{shiny::NS()}}.}

\item{delay_type}{A \code{character} string, either \code{"incubation"} or
\code{"onset_to_isolation"}, selecting which delay distribution to parameterise.
Controls the input IDs, labels and tooltips generated by this function.}

\item{id_prefix}{A \code{character} string prepended to every input ID generated
by this function (e.g. \code{"dct_"}). Allows the inputs to be reused several
times within the same module (e.g. one set per contact tracing strategy)
without clashing. Defaults to \code{""} (no prefix).}

\item{...}{\link{dots} Not used, will throw a warning if arguments are supplied.}
}
\value{
A \code{\link[bslib:accordion]{bslib::accordion()}} object.
}
\description{
Generate \code{\link[bslib:accordion]{bslib::accordion()}} with inputs for parameterising the
\code{incubation_period} or \code{onset_to_isolation} argument in
\code{\link[ringbp:delay_opts]{ringbp::delay_opts()}}
}
\keyword{internal}
