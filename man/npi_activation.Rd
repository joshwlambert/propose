% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{npi_activation}
\alias{npi_activation}
\title{Wrap an intervention value in a time-varying NPI-activation function}
\usage{
npi_activation(value, activation_day)
}
\arguments{
\item{value}{A \code{numeric} scalar in \verb{[0, 1]}: the intervention value applied
once NPIs are active.}

\item{activation_day}{A \code{numeric} scalar: the day of the outbreak on which
NPIs activate. \code{0} means active immediately.}
}
\value{
A \code{function} of a single \code{numeric} argument \code{t}.
}
\description{
Non-pharmaceutical interventions (NPIs) are assumed to activate on a given
day of the outbreak. Before that day the intervention value is 0 (no
contact tracing / no test-driven isolation); on and after it the
user-specified value applies. This mirrors how the \code{symptomatic_traced}
argument in \code{\link[ringbp:event_prob_opts]{ringbp::event_prob_opts()}} and the \code{test_sensitivity} argument
in \code{\link[ringbp:intervention_opts]{ringbp::intervention_opts()}} accept a function of time \code{t}.

\link[ringbp:ringbp]{ringbp} coerces scalar intervention values to constant functions of \code{t}
internally, and only ever evaluates them at the (continuous, strictly
positive) \code{onset} / \code{exposure} times of cases, so \code{activation_day = 0}
reproduces the original scalar behaviour exactly. The strict \verb{t >}
comparison follows the same convention as upstream ringbp usage; it would
only differ from the scalar if the function were ever evaluated at a
discrete \code{t = 0}, which ringbp does not do.
}
\keyword{internal}
