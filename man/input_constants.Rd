% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants.R
\name{input_constants}
\alias{input_constants}
\alias{BASIC_K}
\alias{BASIC_DELAY_SHAPE}
\alias{OFFSPRING_TIP}
\alias{DISP_TIP}
\title{Constants behind the sidebar input controls}
\usage{
BASIC_K

BASIC_DELAY_SHAPE

OFFSPRING_TIP

DISP_TIP
}
\description{
What the Basic-mode controls mean in model terms, and the tooltip text the
offspring inputs share. These are what let a user who does not want to think
in distribution parameters still describe a pathogen.
}
\details{
\strong{\code{BASIC_K}} maps the \code{basic_transmission_variability} radio choices in
\code{\link[=offspring_input]{offspring_input()}} to the Negative Binomial dispersion parameter (\code{size}
in \code{\link[stats:rnbinom]{stats::rnbinom()}}). \code{"homogeneous"} (\code{k = Inf}) is equivalent to a
Poisson offspring distribution, \code{"moderate"} (\code{k = 1}) to a Geometric,
and \code{"high"} (\code{k = 0.1}) gives strong superspreading.

\strong{\code{BASIC_DELAY_SHAPE}} maps the basic delay "variability" radio choices in
\code{\link[=delays_input]{delays_input()}} to the shape parameter of a Gamma distribution (\code{shape} in
\code{\link[stats:rgamma]{stats::rgamma()}}). With the mean held fixed (\code{scale = mean / shape}),
\code{shape} controls the skewness of the tail (skewness = 2 / sqrt(shape)):
\code{"low"} (\code{shape = 20}) is near-symmetric, \code{"moderate"} (\code{shape = 5}) is a
classic right skew, and \code{"high"} (\code{shape = 2}) has a long, heavy tail.

\strong{\code{OFFSPRING_TIP}} and \strong{\code{DISP_TIP}} are the tooltip text for the offspring
distribution inputs, shared by the Advanced transmissibility controls in
\code{\link[=offspring_input]{offspring_input()}} and by the R0 sweep in \code{\link[=r0_seq_input]{r0_seq_input()}}, which offer the
same choice of distribution and so must explain it the same way.
\code{OFFSPRING_TIP} describes the choice of distribution; \code{DISP_TIP} describes
the Negative Binomial dispersion parameter that governs superspreading.
Neither is to be confused with \code{\link[=shape_tip]{shape_tip()}} and \code{\link[=scale_tip]{scale_tip()}}, which are
functions that build tooltip text for a named distribution and delay.
}
\keyword{internal}
