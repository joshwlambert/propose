% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{run_scenario}
\alias{run_scenario}
\title{Simulate one outbreak scenario}
\usage{
run_scenario(params, shared)
}
\arguments{
\item{params}{A canonical parameter list. See \link{canonical}.}

\item{shared}{A named \code{list} of the simulation settings that are held
constant across scenarios: \code{replicates}, \code{initial_cases}, \code{cap_max_days} and
\code{cap_cases}.}
}
\value{
A \link[data.table:data.table]{data.table::data.table}, the output of \code{\link[ringbp:scenario_sim]{ringbp::scenario_sim()}},
carrying its \code{extinct} and \code{cap_cases} attributes.
}
\description{
Deliberately does \strong{not} set the random seed. The caller does, which is what
lets the \emph{\strong{Compare}} page reuse one seed across every scenario in a run and
so make a whole comparison reproducible.

Note that reusing the seed does \strong{not} pair replicates across scenarios:
changing an intervention changes how many random draws each replicate
consumes, so the streams diverge immediately and the measured correlation
between scenarios is near zero. It buys reproducibility, not the variance
reduction that common random numbers would give.
}
\keyword{internal}
