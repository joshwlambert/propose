% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{apply_params}
\alias{apply_params}
\title{Write a snapshot of sidebar input values back into the sidebar}
\usage{
apply_params(session, vals)
}
\arguments{
\item{session}{The \code{session} object passed to function given to
\code{shinyServer}. Default is \code{getDefaultReactiveDomain()}.}

\item{vals}{A named \code{list} of raw input values, as captured by
\code{\link[shiny:reactiveValuesToList]{shiny::reactiveValuesToList()}} when the scenario was added.}
}
\value{
Nothing, called for side-effects from \verb{shiny::update*()} functions.
}
\description{
Restores the scenario a user added so they can edit it, on the \emph{\strong{Compare}}
page's "Load into sidebar" action. It takes the \emph{raw} input snapshot rather
than the canonical parameters (see \link{canonical}) so the sidebar comes back in
the mode the user entered it in, rather than being rewritten into the
Advanced mode.
}
\keyword{internal}
